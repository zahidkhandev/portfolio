import React from "react";
import styled from "styled-components";

function Contact({ theme }) {
  return <Wrappper>Contact</Wrappper>;
}

export default Contact;

const Wrappper = styled.div`
  margin: 20px;
  height: 400px;
`;
